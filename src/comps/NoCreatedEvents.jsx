const NoCreatedEvents = () => (
  <div className="text-center text-gray-400 mt-10">
    <p>No Events Found</p>
    <p>You haven't created any events yet. Click 'Create Event' to get started!</p>
  </div>
);

export default NoCreatedEvents; // ← this is required
